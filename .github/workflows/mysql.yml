# This is a basic workflow to help you get started with Actions

name: MYSQL CI

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    env:
      DB_DATABASE: test_db
      DB_USER: root
      DB_PASSWORD: root

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Start MySQL
        run: |
          mysql -u shashankthemoon -h 34.46.175.15 -P 3307 employees --password=#July1992 -e "select * from departments;"
