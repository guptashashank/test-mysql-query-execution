# This is a basic workflow to help you get started with Actions

name: MYSQL CI

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    env:
      DB_DATABASE: employees
      DB_USER: shashankthemoon
      DB_PASSWORD: '#July1992'

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Start MySQL
        run: |
          mysql -u  $DB_USER -h 34.46.175.15 -P 3307  $DB_DATABASE --password=$DB_PASSWORD < query.sql
